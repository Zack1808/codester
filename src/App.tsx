import React from "react";

import { Slider } from "./components";

const App: React.FC = () => {
  return (
    <div className={`app `}>
      <Slider label="Hello" />
    </div>
  );
};

export default App;
