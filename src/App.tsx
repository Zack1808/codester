import React from "react";

import { Button } from "./components";

const App: React.FC = () => {
  return (
    <div className={`app `}>
      <Button>Hello</Button>
    </div>
  );
};

export default App;
